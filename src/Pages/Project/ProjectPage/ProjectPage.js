import { Box, Typography } from '@mui/material';
import React from 'react';

export default function ProjectPage() {
    return (
        <Box>
            <Typography variant='h1'>ProjectPage</Typography>
        </Box>
    );
}