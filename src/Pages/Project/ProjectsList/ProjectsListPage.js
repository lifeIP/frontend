import { Box, Typography } from '@mui/material';
import React from 'react';

export default function ProjectsListPage() {
    return (
        <Box>
            <Typography variant='h1'>ProjectsListPage</Typography>
        </Box>
    );
}